[package]
name = "artemis"
edition = { workspace = true }
version = { workspace = true }
license = "MIT"

[dependencies]
base64 = { workspace = true }
log = { workspace = true }
core = { path = "../core" }
clap = { version = "4.5.3", features = ["std", "help", "derive"] }
